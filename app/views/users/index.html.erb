<%=link_to 'ユーザ登録', new_user_path, data: { method: :get } %>

<% @users.each do |e| %>
  <p>
      <%= e.uid %>
      <%=link_to '削除', user_path(e), data: { turbo_method: :delete } %>
  </p>
<% end %>

<%=link_to 'ホーム', root_path  %>




